<?xml version="1.0" encoding="utf-8"?>
<!--
  Fondo de la ventana de arranque en frio. Coincide EXACTAMENTE con la
  pantalla de carga de Compose (mismo gradiente + mismo logo redondeado
  centrado) para que el paso de la ventana nativa a Compose sea invisible
  (sin saltos). El logo ya trae las esquinas redondeadas horneadas
  (transparentes), asi que no hace falta tarjeta de fondo.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:type="linear"
                android:angle="270"
                android:startColor="#FF4E7A5E"
                android:centerColor="#FF3E6347"
                android:endColor="#FF2A4D32" />
        </shape>
    </item>
    <item
        android:width="120dp"
        android:height="120dp"
        android:gravity="center"
        android:drawable="@drawable/ic_splash_logo" />
</layer-list>
